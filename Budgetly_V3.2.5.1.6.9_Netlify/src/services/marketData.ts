export type SecuritySuggestion = { symbol: string; companyName: string; exchange: string; currency: 'CAD' | 'USD'; fallbackPrice: number }
export type MarketQuote = { symbol: string; price: number; previousClose: number | null; currency: 'CAD' | 'USD'; timestamp: string; isEstimated?: boolean }
export const STATIC_SECURITIES: SecuritySuggestion[] = [
{ symbol:'AAPL', companyName:'Apple Inc.', exchange:'NASDAQ', currency:'USD', fallbackPrice:298.97 },{ symbol:'MSFT', companyName:'Microsoft Corporation', exchange:'NASDAQ', currency:'USD', fallbackPrice:421.15 },{ symbol:'TSLA', companyName:'Tesla Inc.', exchange:'NASDAQ', currency:'USD', fallbackPrice:181.23 },{ symbol:'NVDA', companyName:'NVIDIA Corporation', exchange:'NASDAQ', currency:'USD', fallbackPrice:119.82 },{ symbol:'AMZN', companyName:'Amazon.com Inc.', exchange:'NASDAQ', currency:'USD', fallbackPrice:189.37 },{ symbol:'GOOGL', companyName:'Alphabet Inc.', exchange:'NASDAQ', currency:'USD', fallbackPrice:173.91 },{ symbol:'SHOP.TO', companyName:'Shopify Inc.', exchange:'TSX', currency:'CAD', fallbackPrice:88.12 },{ symbol:'RY.TO', companyName:'Royal Bank of Canada', exchange:'TSX', currency:'CAD', fallbackPrice:150.44 },{ symbol:'TD.TO', companyName:'Toronto-Dominion Bank', exchange:'TSX', currency:'CAD', fallbackPrice:81.22 },{ symbol:'BNS.TO', companyName:'Bank of Nova Scotia', exchange:'TSX', currency:'CAD', fallbackPrice:65.88 },{ symbol:'BMO.TO', companyName:'Bank of Montreal', exchange:'TSX', currency:'CAD', fallbackPrice:124.31 },{ symbol:'VFV.TO', companyName:'Vanguard S&P 500 Index ETF', exchange:'TSX', currency:'CAD', fallbackPrice:129.03 },{ symbol:'XEQT.TO', companyName:'iShares Core Equity ETF Portfolio', exchange:'TSX', currency:'CAD', fallbackPrice:32.65 },]
export const searchSecurities = async (query: string) => { const q=query.trim().toLowerCase(); if(!q) return []; return STATIC_SECURITIES.filter((i)=>i.symbol.toLowerCase().includes(q)||i.companyName.toLowerCase().includes(q)).slice(0,12)}
export const getQuote = async (symbol: string)=>{const res=await getBatchQuotes([symbol]); if(!res.quotes[0]) throw new Error('Quote unavailable'); return res.quotes[0]}
export const getBatchQuotes = async (symbols: string[]) => { const unique=[...new Set(symbols.map((s)=>s.trim().toUpperCase()).filter(Boolean))]; if(!unique.length) return {quotes:[],failed:[] as string[]}; try { const response=await fetch('/api/market-quotes',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({symbols:unique})}); if(response.status===429) return {quotes:[],failed:unique,rateLimited:true}; if(response.status===503) return {quotes:[],failed:unique,notConfigured:true}; if(!response.ok) throw new Error('failed'); const data=await response.json() as {quotes?:MarketQuote[];failed?:string[]}; return {quotes:data.quotes??[], failed:data.failed??[]}; } catch { const map=new Map(STATIC_SECURITIES.map((i)=>[i.symbol,i])); const quotes=unique.flatMap((s)=>{const m=map.get(s); return m?[{symbol:m.symbol, price:m.fallbackPrice, previousClose:null, currency:m.currency, timestamp:new Date().toISOString(), isEstimated:true}]:[]}); return {quotes, failed: unique.filter((s)=>!map.has(s))}; }}
